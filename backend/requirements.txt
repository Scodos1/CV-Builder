fastapi==0.115.*
uvicorn[standard]==0.30.*
pyjwt==2.8.*
python-multipart==0.0.12
sqlalchemy==2.0.*
# Postgres prod driver (SQLite needs no driver)
psycopg2-binary==2.9.*
# Legacy only: verify old db.json bcrypt hashes (new hashes use stdlib PBKDF2)
# passlib[bcrypt]==1.7.*
# LLM providers (install one or both; Groq is free)
groq==0.13.*
# openai==1.*.*
# Optional: server-side PDF export
# weasyprint==62.*.*
